# ============================================================================
# CrowdUp Environment Configuration
# ============================================================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
#
# Required in production: All variables marked [REQUIRED]
# Optional in development: Variables marked [OPTIONAL] have fallbacks
# ============================================================================

# ----------------------------------------------------------------------------
# Supabase Configuration
# ----------------------------------------------------------------------------
# Get these from your Supabase project dashboard:
# https://supabase.com/dashboard/project/_/settings/api

# [REQUIRED] Supabase project URL
# Example: https://abcdefghijklmnop.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# [REQUIRED] Supabase anon/public key (safe to expose to browser)
# Found in: Settings > API > Project API keys > anon/public
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# [REQUIRED] Supabase service_role key (KEEP SECRET - server only)
# Found in: Settings > API > Project API keys > service_role
# WARNING: This key bypasses Row Level Security - never expose to clients
SUPABASE_SECRET_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ----------------------------------------------------------------------------
# JWT Configuration
# ----------------------------------------------------------------------------

# [REQUIRED] JWT signing secret
# Generate with: openssl rand -base64 32
# Minimum 32 characters for security
JWT_SECRET=your-secure-random-secret-at-least-32-chars

# ----------------------------------------------------------------------------
# Google OAuth Configuration
# ----------------------------------------------------------------------------
# Get these from Google Cloud Console:
# https://console.cloud.google.com/apis/credentials

# [OPTIONAL] Google OAuth Client ID
# Required if using Google Sign-In
NEXT_PUBLIC_GOOGLE_CLIENT_ID=123456789012-abcdefghijklmnop.apps.googleusercontent.com

# [OPTIONAL] Google OAuth Client Secret
# Required if using Google Sign-In
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret

# ----------------------------------------------------------------------------
# Application Configuration
# ----------------------------------------------------------------------------

# [OPTIONAL] Application URL (defaults to http://localhost:3000 in dev)
# Set to your production URL in deployment
NEXT_PUBLIC_APP_URL=http://localhost:3000

# [OPTIONAL] CORS allowed origins (comma-separated)
# If not set, defaults to localhost:3000 and NEXT_PUBLIC_APP_URL
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://crowdup.io

# ----------------------------------------------------------------------------
# Environment
# ----------------------------------------------------------------------------

# [AUTO] Node environment (set automatically by Next.js)
# Do not set manually unless testing
# NODE_ENV=development
